spring:
  datasource:
    data-username: com.mysql.cj.jdbc.Driver
    username: rds_mysql_db
    password: liulvhua
    url: jdbc:mysql://rm-bp17c6zg7kwds7o27125010.mysql.rds.aliyuncs.com:3306/mall?characterEncoding=utf-8&useSSL=false
  redis:
    host: 127.0.0.1
    port: 6379
mybatis:
  configuration:
    map-underscore-to-camel-case: true
    # 控制台日志配置
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  mapper-locations: classpath:mappers/*.xml
logging:
  pattern:
    console: "[%thread] %-5level %logger{36} - %msg%n"
server:
  servlet:
    session:
      timeout: 120